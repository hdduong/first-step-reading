{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd \"${CLAUDE_PROJECT_DIR:-.}\" && git rev-parse --is-inside-work-tree >/dev/null 2>&1 && git pull --ff-only --quiet 2>/dev/null || true",
            "timeout": 30,
            "statusMessage": "Pulling latest (Codex may have pushed)…"
          }
        ]
      }
    ]
  }
}
