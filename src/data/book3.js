// Book 3 — placeholder. Fill `lessons` with the same shape as book1.js
// (id, family, title, pages, words[{word, pic}], sight[], sentences[{page, pic, words[]}]).
// The phonics engine now handles any short vowel — families like -et/-en, -ig/-in,
// -op/-og, -ug/-un all work — so just set `vowel` (e.g. "Short Vowel I") and fill in.
export default {
  id: "book3",
  step: 1,
  vowel: "Book 3",
  pageRange: "",
  comingSoon: true,
  lessons: [],
};
